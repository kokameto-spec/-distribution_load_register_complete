name: distribution_load_register_final
description: سجل أحمال الموزعات - النسخة السحابية
publish_to: 'none'

version: 1.4.0+4

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  provider: ^6.1.5+1
  intl: ^0.19.0

  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12

  connectivity_plus: ^6.1.5
  crypto: ^3.0.6
  http: ^1.2.2

  pdf: ^3.11.3
  printing: ^5.14.2

  excel: ^4.0.6
  file_saver: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/company_logo.png